in List {
  void pushAll(List<T> all) {
    for (var i = 0, n = all.size(); i < n; i++) {
      push(all[i])
    }
  }
}

namespace prettyPrint {
  string plural(int value) {
    return value == 1 ? "" : "s"
  }
}
